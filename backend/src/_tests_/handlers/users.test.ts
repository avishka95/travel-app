// import { mockRequest, mockResponse } from "../../_mocks_";
// import { getUserById, getUsers } from "../../handlers/users";

describe('test should be implemented', () => {
    it('should be implemented', () => {
        expect(true).toBe(true);
    });
});
// describe('getUsers', () => {
//     it('should return an array of users', () => {
//         getUsers(mockRequest, mockResponse);
//         expect(mockResponse.send).toHaveBeenCalledWith([]);
//     });
// });

// describe('getUsersById', () => {
//     it('should return an empty object', () => {
//         const mockRequestWithId = { ...mockRequest, params: { id: 200 } } as any;
//         getUserById(mockRequestWithId, mockResponse);
//         expect(mockResponse.send).toHaveBeenCalledWith({});
//     })
// });
